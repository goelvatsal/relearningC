#include <stdio.h>
int main(void) {
    // int i = 0 , j = 0;
    // float nextMult = i + j - i % j;

    printf("nextMult = %d.\n", 365 + 7 - 365 % 7);
    printf("nextMult = %d.\n", 12258 + 23 - 12258 % 23);
    printf("nextMult = %d.\n", 996 + 4 - 996 % 4);
}